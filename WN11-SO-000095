<#
.SYNOPSIS
    The Smart Card removal option must be configured to Force Logoff or Lock Workstation. Unattended systems are susceptible to unauthorized use and must be locked. Configuring a system to lock when a smart card is removed will ensure the system is inaccessible when unattended.


.NOTES
    Author          : Aaron M Brown
    LinkedIn        : https://www.linkedin.com/in/aaron-m-brown-9a2b76258/
    GitHub          : https://github.com/AaronMBrown01
    Date Created    : 2026-08-18
    Last Modified   : 2026-08-18
    Version         : 1.0
    CVEs            : N/A
    Plugin IDs      : N/A
    STIG-ID         : WN11-SO-000095
    Documentation   : https://stigaview.com/products/win11/v2r8/WN11-SO-000095/

.TESTED ON
    Date(s) Tested  : 
    Tested By       : 
    Systems Tested  : 
    PowerShell Ver. : 

.USAGE
    Put any usage instructions here.
    Example syntax:
    PS C:\> .\STIG-ID-WN11-SO-000095.ps1 
#>

# YOUR CODE GOES HERE


# Ensure administrative privileges
if (-not ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) {
    Write-Error "This script must be run as an Administrator."
    exit 1
}

$RegistryPath = "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon"
$ValueName    = "SCRemoveOption"

# Value Options:
# "1" = Lock Workstation
# "2" = Force Logoff
$DesiredValue = "1"

# Create registry key path if missing
if (-not (Test-Path -Path $RegistryPath)) {
    New-Item -Path $RegistryPath -Force | Out-Null
}

# Apply the security policy value as a String (REG_SZ)
Set-ItemProperty -Path $RegistryPath -Name $ValueName -Value $DesiredValue -Type String -Force

Write-Host "STIG WN11-SO-000095 applied successfully. SCRemoveOption set to '$DesiredValue' (Lock Workstation)." -ForegroundColor Green



